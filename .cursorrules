# 剪贴板内容匹配检测器 - 项目开发规范

## 🌏 语言规范
- **注释语言**: 统一使用中文注释，这是一个中文项目
- **变量命名**: 代码中的变量名可以使用英文，但注释必须是中文
- **文档语言**: README.md、测试说明等文档使用中文

## 🏗️ 架构原则
- **模块化设计**: UI层与业务逻辑层完全分离
- **单一职责**: 每个类只负责一个功能领域
- **核心模块**: 
  - `core.py`: 业务逻辑和数据处理
  - `ui.py`: 用户界面组件
  - `main.py`: 应用程序入口和控制器
- **配置分离**: 配置项统一在Config类中管理

## 💻 代码风格
- **命名规范**:
  - 类名: PascalCase (如 `FieldManager`)
  - 函数名: snake_case (如 `add_field`)
  - 常量: UPPER_CASE (如 `MAX_FIELDS`)
  - 私有方法: 以下划线开头 (如 `_handle_save`)
- **导入顺序**: 标准库 → 第三方库 → 本地模块
- **行长度**: 不超过88字符
- **空行**: 类之间2个空行，方法之间1个空行

## 🧪 测试规范
- **测试覆盖**: 核心模块测试覆盖率不低于65%
- **测试文件**: 
  - 测试文件命名: `test_*.py`
  - 测试类命名: `Test*` (如 `TestFieldManager`)
  - 测试方法命名: `test_*` (如 `test_field_creation`)
- **测试原则**: 
  - 测试应该独立，不依赖其他测试
  - 每个测试只验证一个功能点
  - 使用有意义的断言消息

## 🔄 重构原则
- **渐进式重构**: 代码变动尽可能小
- **功能保持**: 重构后必须保持原有功能完全不变
- **向后兼容**: 配置文件格式保持向后兼容
- **测试先行**: 重构前确保现有测试通过

## 🐛 错误处理
- **优雅降级**: 错误不应该导致程序崩溃
- **日志记录**: 使用DebugSystem记录关键操作和错误
- **用户友好**: 错误信息应该对用户有意义

## 📝 字段管理规范
- **无上限**: 字段数量不设上限，用户可以添加任意数量的字段
- **最小限制**: 至少保留1个字段
- **持久化**: 字段配置自动保存到JSON文件

## ⌨️ 热键管理规范
- **默认热键**: F8开始监听，F9停止监听
- **配置验证**: 热键配置必须经过有效性验证
- **配置修复**: 无效配置自动修复为默认值

## 🎯 性能要求
- **响应性**: UI操作响应时间 < 100ms
- **轮询间隔**: 剪贴板轮询间隔500ms
- **内存管理**: 及时清理不用的对象和事件监听器

## 🔍 调试规范
- **开发环境**: 显示详细调试信息
- **生产环境**: 只记录警告和错误
- **日志文件**: 调试信息同时输出到控制台和debug.log文件
